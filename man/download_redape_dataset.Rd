% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redape.R
\name{download_redape_dataset}
\alias{download_redape_dataset}
\title{Download the curated Redape GeoTab dataset (Vaz et al 2023)}
\usage{
download_redape_dataset(
  dest_dir,
  dataset_doi = .REDAPE_GEOTAB_DOI,
  include_rtf = FALSE,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{dest_dir}{Destination directory for the JSON files.}

\item{dataset_doi}{DOI of the dataset (default: the Vaz 2023 dataset).}

\item{include_rtf}{If \code{TRUE}, also download the original RTF
profile sheets (default \code{FALSE}; the JSON files alone
are enough for classification).}

\item{overwrite}{If \code{TRUE}, re-download files that already
exist locally.}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
Character vector of paths to the downloaded files.
}
\description{
Enumerates the dataset via the Dataverse API and downloads all
JSON profile files (the structured / interoperable format used
by the curators) into \code{dest_dir}. Skips files already
present unless \code{overwrite = TRUE}.
}
\references{
Vaz, G. J., Silva Jr, A. F., & Silva Neto, L. de F. da
  (2023). Brazilian soil data for taxonomic classification. Redape, V1.
  \doi{10.48432/PYKKA7}.
}
