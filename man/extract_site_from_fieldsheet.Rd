% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vlm-extract.R
\name{extract_site_from_fieldsheet}
\alias{extract_site_from_fieldsheet}
\title{Extract site metadata from a field-sheet image}
\usage{
extract_site_from_fieldsheet(
  pedon,
  image_path,
  provider,
  max_retries = 3L,
  overwrite = FALSE,
  prompt_name = "extract_site_metadata",
  schema_name = "site"
)
}
\arguments{
\item{pedon}{A \code{\link{PedonRecord}}.}

\item{image_path}{Path to the field-sheet image.}

\item{provider}{A chat provider from \code{\link{vlm_provider}} (or
a \code{\link{MockVLMProvider}} for testing).}

\item{max_retries}{Integer; how many times to re-prompt on
validation failure. Default 3.}

\item{overwrite}{If \code{TRUE}, lower-authority values are allowed
to clobber higher-authority ones. Default \code{FALSE}.}

\item{prompt_name}{Override the default prompt template
(\code{"extract_horizons"}).}

\item{schema_name}{Override the default schema (\code{"horizon"}).}
}
\value{
Invisibly, the mutated \code{pedon}.
}
\description{
Sends a photographed / scanned field sheet to a multimodal VLM and
merges the extracted site-level metadata (lat, lon, elevation,
parent material, land use, etc.) into \code{pedon$site}. Existing
fields are preserved unless \code{overwrite = TRUE}; only NULL
fields are filled.
}
