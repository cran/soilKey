% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectra-ossl.R
\name{make_synthetic_pedon_with_spectra}
\alias{make_synthetic_pedon_with_spectra}
\title{Build a synthetic PedonRecord with attached spectra (testing aid)}
\usage{
make_synthetic_pedon_with_spectra(
  n_horizons = 5L,
  wavelengths = 350:2500,
  seed = 1L
)
}
\arguments{
\item{n_horizons}{Integer number of horizons (default 5).}

\item{wavelengths}{Integer vector of wavelengths (default
\code{350:2500}).}

\item{seed}{Integer applied through \code{\link[withr]{with_seed}}
so the synthetic spectra are reproducible \emph{without}
mutating the caller's global RNG state (the prior stream is
restored on exit). Pass \code{NULL} to draw from the current
RNG stream untouched. Default \code{1L} keeps the
bit-for-bit identical output that earlier soilKey releases
produced, but does so via \code{with_seed()} (CRAN policy:
never call \code{set.seed()} on the caller's RNG).}
}
\value{
A \code{\link{PedonRecord}} with a \code{$spectra$vnir}
        matrix attached.
}
\description{
Generates a small, deterministic \code{\link{PedonRecord}} with
\code{n_horizons} horizons and a Vis-NIR spectral matrix
(\code{350:2500} nm). Useful for exercising
\code{\link{fill_from_spectra}} in tests and vignettes.
}
