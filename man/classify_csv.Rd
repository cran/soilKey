% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-pedon-csv.R
\name{classify_csv}
\alias{classify_csv}
\title{Classify a horizon spreadsheet in all three systems - one file, one line}
\usage{
classify_csv(
  file,
  site = NULL,
  sep = "auto",
  systems = c("wrb", "sibcs", "usda")
)
}
\arguments{
\item{file}{Path to a \code{.csv} (comma) or \code{.tsv} (tab) file, one row
per horizon.}

\item{site}{Optional named list of site metadata (see \code{\link{PedonRecord}}).
Defaults to \code{list(id = <file base name>)}.}

\item{sep}{Field separator. \code{"auto"} (default) uses a tab for
\code{.tsv} files and a comma otherwise.}

\item{systems}{Character vector of systems to run; any of \code{"wrb"},
\code{"sibcs"}, \code{"usda"} (default: all three).}
}
\value{
A one-row \code{data.frame} with one column per system.
}
\description{
The shortest path from a spreadsheet to an answer. Reads \code{file} with
\code{\link{read_pedon_csv}} and returns the WRB 2022 / SiBCS / USDA names as
a one-row \code{data.frame}. Missing attributes are handled silently
(\code{on_missing = "silent"}).
}
\details{
The full \code{\link{ClassificationResult}} objects (with the key trace,
evidence grade, qualifiers, ...) and the parsed \code{\link{PedonRecord}} are
attached as attributes \code{"results"} and \code{"pedon"} for anyone who
wants to dig deeper.
}
\examples{
f <- system.file("extdata", "perfil_exemplo.csv", package = "soilKey")
classify_csv(f)
}
\seealso{
\code{\link{read_pedon_csv}}, \code{\link{classify_all}}.
}
